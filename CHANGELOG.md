# Changelog

## Unreleased

### Fixed
- Fixed `clighl conversations send --type Email` so email sends include body content in the shape HighLevel accepts.
- Fixed contact resolution so `--contact` can use an exact contact ID directly in non-interactive flows.

### Docs
- Updated the README email-send examples to document working email usage and direct contact-ID sends.
